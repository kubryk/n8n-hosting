# n8n Workers Cluster

Standalone worker nodes for distributed n8n processing. Deploy on separate servers for horizontal scaling.

## Deployment

Deploy this configuration on additional servers to create a worker cluster:

```bash
docker-compose up -d
```

## Configuration

Before deployment, configure .env file
